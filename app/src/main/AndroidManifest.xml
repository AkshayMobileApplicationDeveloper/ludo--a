<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.afridi.ludo">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

    <!-- Main Activity -->
        <activity
            android:name="com.afridi.ludo.Game"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Additional Activities -->
        <activity
            android:name="com.afridi.ludo.winBlue"
            android:exported="true" />

        <activity
            android:name="com.afridi.ludo.winGreen"
            android:exported="true" />

        <activity
            android:name="com.afridi.ludo.PlayerSelect"
            android:exported="true" />

        <activity
            android:name="com.afridi.ludo.winRed"
            android:exported="true" />

        <activity
            android:name="com.afridi.ludo.winYellow"
            android:exported="true" />

    </application>

</manifest>
